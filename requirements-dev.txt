# Development and CI testing dependencies for Algo-Trading-Skills
-r requirements.txt
pytest>=9.1.1

# The agentskills.io reference validator is deliberately NOT listed here: it requires
# Python 3.11+, while this library supports 3.10+, so pinning it would make the whole
# dev install fail on 3.10. Install it separately on 3.11 or newer:
#     pip install skills-ref
#     agentskills validate skills/<skill-name>
