[pytest]
# A bare `pytest` from the repo root runs only the root suite. The 500-odd skill suites
# under skills/*/scripts share module names (two skills each ship a circuit_breaker.py),
# so they must run isolated: python tools/run_all_tests.py, or one at a time with
# python -m unittest discover -s skills/<name>/scripts
testpaths = tests
