# Reporting Workflow

1. Aggregate returns for the strategy at a consistent frequency (e.g. daily).
2. Instantiate `StandardizedTearsheetGenerator`.
3. Compute the report dictionary and format it into a table/UI.
