# Pre-Flight Checklist

- [ ] Are all vendor-specific identifiers registered in the cross-reference mapping table?
- [ ] Do forward lookups (vendor → canonical) resolve correctly for all vendors?
- [ ] Do reverse lookups (canonical → vendor) return correct vendor-specific identifiers?
- [ ] Are ambiguous one-to-many mappings detected and flagged?
