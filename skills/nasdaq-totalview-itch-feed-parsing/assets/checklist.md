# Pre-Flight Checklist

- [ ] Is Big-Endian `>` specifier used for struct unpacking?
- [ ] Is raw integer price divided by $10,000.0$?
- [ ] Are Add ('A'), Execute ('E'), Cancel ('X'), Delete ('D') messages handled?
- [ ] Is L3 order book state tracked in `orders_by_ref` map?
